/** @type {import('next').NextConfig} */
const nextConfig = { // Configuração para exportar o aplicativo como um site estático
    // Você pode personalizar isso conforme necessário
    // Consulte a documentação do Next.js para mais opções: https://nextjs.org/docs/api-reference/next.config.js/output
  output:"export"
}

export default nextConfig;
